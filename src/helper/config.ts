// sitemap for i18n locals, https://docs.astro.build/en/guides/integrations-guide/sitemap/#i18n

// import { siteConfig } from "../config";
// import { ALL_LANGUAGES } from "../constants/constants";

// const activeLocales: string[] =
// 	siteConfig.supportedLangs && siteConfig.supportedLangs.length > 0
// 		? [...siteConfig.supportedLangs]
// 		: [...ALL_LANGUAGES];

// if (!activeLocales.includes(siteConfig.lang)) {
// 	activeLocales.push(siteConfig.lang);
// }

// export const sitemapLocales = Object.fromEntries(
// 	activeLocales.map((locale) => [locale, locale.replace("_", "-")]),
// );
